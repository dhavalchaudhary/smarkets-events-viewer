import { transformEventAPIResponse } from './api'

describe('api helpers', () => {
  describe('transformEventAPIResponse', () => {
    it.todo('transorms the response and returns valid data')
  })
  describe('transformMultipleEventsAPIResponse', () => {
    it.todo('transorms the response and returns valid data')
  })
})
