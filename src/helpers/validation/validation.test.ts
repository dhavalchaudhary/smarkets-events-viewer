import { getEventIdsWithData } from './validation'

describe('Validation Helpers', () => {
  describe('getEventIdsWithData', () => {
    it.todo('returns all event id whose data is fetched')
  })
  describe('isEventsDataPresent', () => {
    it.todo('validates if all events data has been fetched')
  })
})
